//Initializing the constants required for calculating the area of the triangle
const base = 10;
const height = 5;

//Calculating the area of the triangle
let areaOfTriangle = (1 / 2) * base * height;

//Logging the area of the triangle to the console
console.log(`Area of the Triangle =${areaOfTriangle}`);
